import {chromium} from "playwright";

describe(`Local testing of generated playwright scripts`, () => {
    jest.setTimeout(3000 * 100);

    test('Local testing of https://www.demoblaze.com logout use case', async () => {

    })

    test('Local testing of https://www.demoblaze.com login use case', async () => {

    })
})